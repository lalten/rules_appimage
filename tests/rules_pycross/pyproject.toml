[project]
dependencies = ["humanize>=4.9.0"]
requires-python = ">=3.8"
name = "pycross_compat"
version = "0.0.1"
description = "rules_appimage / rules_pycross compatibility test"
readme = "README.md"
license = { text = "MIT" }

[tool.pdm]
distribution = false
