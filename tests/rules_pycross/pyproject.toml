[project]
dependencies = ["humanize>=4.11.0"]
requires-python = ">=3.9"
name = "pycross_compat"
version = "0.0.1"
description = "rules_appimage / rules_pycross compatibility test"
readme = "README.md"
license = { text = "MIT" }

[tool.pdm]
distribution = false
